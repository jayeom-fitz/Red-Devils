const { sql, pool } = require("../");

// ==========================|| CONTENT DB ||========================== //
const DB_NAME = "[content]";

// ==========================|| SELECT ||========================== //

module.exports = {};
